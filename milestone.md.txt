# milestone.md

## Current Status

NjeKlik is a connected pre-production auction platform currently deployed at `https://auctionsq.vercel.app`. The core security hardening work for profile updates, bid writes, order delivery confirmation, live-auction uniqueness, public auction exposure, bid moderation scope, and bounded list reads has been implemented, deployed, and verified locally and against the live Supabase project `ewuvavurygndpgarrdfw`.

Active task on 2026-06-17:
- Completed an auth/API limit, vulnerability, and scale-readiness hardening pass:
  - added server-side app rate limits backed by `public.app_rate_limits` and the service-role-only `check_app_rate_limit` RPC.
  - rate-limited sign-in, sign-up, Google OAuth start, password reset, password update, profile update, bid placement, admin actions, and the auction finalizer API.
  - enforced 8-character minimum passwords consistently in auth forms and server actions.
  - added global browser security headers: CSP, referrer policy, nosniff, frame deny, permissions policy, and production HSTS.
  - narrowed remaining broad profile/category reads to explicit columns.
  - resolved the npm audit finding by updating the lockfile transitive dependency resolution.
  - applied the Supabase rate-limit migration to linked project `ewuvavurygndpgarrdfw` and verified table/function creation.
  - Supabase advisors show no new rate-limit migration issues; remaining warnings are the known intentional authenticated RPCs plus dashboard-only leaked-password protection.
- Latest local checks passed:
  - `node --test tests\*.test.mjs`: 35/35 tests.
  - `npm run lint`: passed.
  - `npm run build`: passed.
  - `npm audit --json`: 0 vulnerabilities.
  - `git diff --check`: no whitespace errors; only Windows LF-to-CRLF warnings.
- Removed the desktop public sidebar promo card (`Fito me shume`) before VPS deployment prep.

Active task on 2026-06-18:
- Fixed broken product thumbnails after VPS/domain deployment:
  - added a shared `SafeImage` component that falls back to the local NjeKlik product image when a product image URL is empty or fails to load.
  - replaced raw product image tags in admin products/auctions, admin orders, customer orders/wins/profile activity, public auction cards, auction detail, and winners pages.
  - removed remaining Unsplash fallback URLs from product surfaces so image rendering stays aligned with the current CSP.
- Latest checks passed:
  - `node --test tests\*.test.mjs`: 35/35 tests.
  - `npm run lint`: passed.
  - `npm run build`: passed.
  - `npm audit --json`: 0 vulnerabilities.
  - `git diff --check`: no whitespace errors; only Windows LF-to-CRLF warnings.

Active task on 2026-06-19:
- Investigated the production `/login` client-side exception after signup confirmation:
  - fresh browser production `/login` load passed.
  - invalid credential submit passed and showed the expected inline error.
  - temporary confirmed-user login passed and redirected to the homepage; temporary user was deleted after validation.
  - root risk found: auth pages were still prerendered/static and served with long shared-cache headers, which can leave stale server-action HTML/chunk references after deployments.
- Fixed auth route caching:
  - added `(auth)` route-group layout with `force-dynamic`, `revalidate = 0`, and `force-no-store`.
  - added explicit `Cache-Control: no-store, no-cache, max-age=0, must-revalidate` headers for login, register, reset-password, setup-required, and auth callback routes.
  - added a static regression test that auth pages remain no-store/dynamic.
- Latest checks passed before commit:
  - `node --test tests\*.test.mjs`: 36/36 tests.
  - `npm run lint`: passed.
  - `npm run build`: passed; auth pages now build as dynamic routes.
  - local production `curl -I` confirmed `/login`, `/register`, and `/reset-password` return `Cache-Control: no-store, no-cache, max-age=0, must-revalidate`.

Active task on 2026-06-22:
- Diagnosed production auth redirect failures after moving to `xn--njklik-qva.com`:
  - confirmation email links were still able to land on `localhost:3000`, causing Supabase PKCE verifier errors outside the original signup browser.
  - Google OAuth callback could surface as a raw nginx 502 if the callback route threw instead of returning a controlled auth error.
- Implemented repo-side auth hardening:
  - added `getSiteUrl()` so production auth redirects require `NEXT_PUBLIC_SITE_URL` instead of silently falling back to localhost.
  - sign-up now passes `emailRedirectTo` to Supabase.
  - added `/auth/confirm` as a no-store token-hash confirmation endpoint alias.
  - callback route now handles provider errors and unexpected exceptions by redirecting to `/login?error=...`.
  - added static regression coverage for configured production auth redirects and `/auth/confirm` cache headers.
- Remaining dashboard-side requirement:
  - Supabase Auth email templates must use the production domain and token-hash confirmation links.
  - Google OAuth provider must keep the Supabase callback URL in Google Cloud and the production app callback in Supabase redirect URLs.

Active task on 2026-06-09:
- Implemented client auth/account access request batch:
  - login and registration now include a Google OAuth button that redirects through Supabase and the existing `/auth/callback` route.
  - login and registration now share a split-screen auth design on desktop with NjeKlik visuals, compact mobile layout, and the real Google logo asset.
  - desktop login now places the sign-in form on the left, keeps the image panel on the right, removes the crossed-out login helper/trust text, and keeps the image-panel copy as aligned white text without a logo overlay.
  - Vercel's initial Git-triggered production deployment for `17c6f4a` stalled in `INITIALIZING` with no build logs; a manual `npx vercel deploy --prod --yes` created ready deployment `dpl_2xfLtUu9a6trfm3fY9rFPUMkeT8n` and re-aliased `https://auctionsq.vercel.app`.
  - public mobile menu now shows `Profili` for logged-in clients and `Paneli Admin` for admins so users can return to account pages after visiting Ballina.
  - normal clients now land on Ballina after login/Google OAuth by default; profile completion remains required only before bidding or when the user opens Profili.
  - confirmation email sender branding is not a repo-only change; it requires Supabase Auth custom SMTP/from-name and email template configuration once the production domain/email is ready.
- Implemented the client mobile/UI request batch:
  - home hero now uses the provided landscape product image as a subtle warm background.
  - global theme now forces NjeKlik light colors even when a device/browser is in dark mode.
  - mobile public menu is now controlled and closes after tapping Ballina/Ankandet/Si funksionon.
  - admin overview cards stack winner/auction/bid values on mobile to avoid clipped right-side content.
  - auction detail mobile order is now image, product/bid panel, description, trust notes, bid history, related auctions.
  - related auctions on detail pages use a two-column mobile grid.
  - product image upload now has client/server validation for file type, 5MB per image, 8 images max, and 24MB total, with a 30MB Server Actions body limit to avoid client-side crashes.
  - product image uploads now clean up partial storage uploads if a later file upload fails.
- Cleaned up the customer dashboard/profile area:
  - `/dashboard` no longer renders the stale dark overview screen and redirects to `/dashboard/profile`.
  - normal user sign-in, auth callback, and non-admin admin-route fallback now land on `/dashboard/profile`.
  - removed the redundant customer "Permbledhje" dashboard nav item.
  - restyled the customer dashboard shell, profile, addresses, bids, won auctions, orders, password form, and order-address editor to match the current warm NjeKlik design.
  - corrected the won-auction order details link to point to `/dashboard/orders`.
- Consolidated admin products and auctions into one workflow page:
  - `/admin/auctions` now shows product inventory with filters for unlisted, listed, and processing.
  - the right-side card creates products directly on the same page.
  - each active unlisted product can be listed into a 24h/48h/72h auction with minimum bid, minimum increment, and auto-relist.
  - products can be activated/deactivated from the same list; inactive products cannot be listed into auctions.
  - inactive product auction rules are enforced server-side for both new auctions and relists.
  - completed product stats now show small sold and cancelled order counts.
  - `/admin/products` redirects to `/admin/auctions`.
  - removed the redundant `Produktet` link from the admin sidebar.
  - product rows use a cleaner two-column action layout, without redundant active/inactive badges.
  - safe product deletion is exposed from the combined page for products without auction or order history.
  - blocked delete/relist actions now show clear admin-facing reasons instead of disappearing silently.
- Hid deactivated products from public auction surfaces:
  - home auction grid.
  - auctions listing.
  - ending-soon listing.
  - category counts.
  - auction detail pages now 404 if the related product is inactive.
- Improved desktop public layout:
  - left sidebar is sticky on desktop and no longer scrolls away leaving an empty rail.
  - desktop sidebar rail is wider so nav items align and breathe better.
  - admin sidebar CTA now links to the combined products/auctions workflow.
  - auctions listing uses the same two-column mobile product grid as the homepage.
- Fixed admin relist/delete rules:
  - delivered and cancelled orders no longer block relisting; only open order statuses block it.
  - product delete is permanently available and removes linked auctions, bids, orders, and product image files.
- Implemented client-requested mobile/home adjustments from the Albanian PDF:
  - smaller hero with old hero image removed.
  - active products appear before the "Si funksionon" section.
  - delivery info added: Kosove 3 EUR / 1-2 business days; Shqiperi 5 EUR / 4-5 business days.
  - active product cards use a two-column mobile grid where data exists.
- Corrected brand direction back to NjeKlik after user clarification.
  - `BrandLogo` now uses NjeKlik logo assets directly.
  - Generated transparent-background variants from the provided NjeKlik logo files:
    - `public/brand/njeklik-mark-transparent.png`
    - `public/brand/njeklik-wordmark-transparent.png`
  - Removed visible `AuctionSq` references from `src`.
- Fixed admin order status UX:
  - status select refreshes the server-rendered view after successful update.
  - delivered orders no longer force products to inactive.
  - delivered/cancelled orders are hidden from the active admin orders page.
- Added auction auto-relist:
  - `auctions.auto_relist` boolean added.
  - admin create/toggle UI added per auction.
  - expired no-bid auctions with auto-relist enabled create a new 24h auction server-side.
  - linked Supabase project migration applied with `npx supabase db query --linked --file ...`.
- Added admin cleanup controls:
  - completed/non-live auction bids can be deleted from admin bids.
  - customer accounts can be deleted only when they are not admin, have no open orders, and have no active bids in live auctions.
- Latest local checks passed:
  - `node --test tests\*.test.mjs`: 27/27 tests.
  - `npm run lint`: passed.
  - `npm run build`: passed.
  - `git diff --check`: no whitespace errors; only Windows LF-to-CRLF warnings.

Baseline release-readiness check on 2026-06-08:
- `node --test tests\*.test.mjs` passed: 18/18 tests.
- `npm run lint` passed with no ESLint warnings or errors.
- `npm run build` passed.
- `npm audit --json` reported 0 vulnerabilities.

Active release-completion process:
1. Supabase advisor gate: passed via Supabase CLI, with documented residuals.
2. Transparent NjeKlik logo assets: passed.
3. Mobile responsiveness and UX pass: passed.
4. Production admin CRUD validation with temporary data: passed.
5. Two-user auction lifecycle validation: passed.
6. Performance/logical review: passed.
7. Final review, deployment, and production browser validation: passed.

Supabase advisor gate on 2026-06-08:
- Initial security advisor MCP call failed with permission error `-32600`.
- Initial performance advisor MCP call failed with permission error `-32600`.
- Initial local environment had no `supabase` CLI in PATH.
- A later final retry through MCP still failed with the same `-32600` permission error for both advisor types.
- Later CLI access was restored with `npx supabase login` and `npx supabase link --project-ref ewuvavurygndpgarrdfw`.
- Correct remote command is `npx supabase db advisors --linked`; without `--linked`, the CLI tries local Postgres on `127.0.0.1:54322`.
- Applied advisor hardening migrations directly with `npx supabase db query --linked --file ...` because remote migration history is not aligned with local migration files.
- Fixed real advisor findings:
  - mutable search path on `update_updated_at_column`.
  - broad public listing policy on `storage.objects` for `product-images`.
  - direct execution of internal trigger/maintenance security-definer functions.
  - missing indexes on FK columns for auctions, audit logs, bids, orders, and products.
  - RLS `auth.uid()` initplan warnings.
  - duplicate permissive policy warnings after splitting anon/authenticated read policies.
- Final production E2E passed after RLS changes, including admin CRUD, bidding, finalization, orders, and audit logs.
- Cleaned up all advisor-validation QA data and users.
- Remaining Supabase advisor residuals:
  - authenticated security-definer warnings for intentional RPC endpoints: `place_bid`, `update_own_profile`, `confirm_order_delivery`, plus `current_user_is_admin` used by authenticated admin RLS policies.
  - Auth leaked-password protection is disabled and must be enabled in Supabase Dashboard Auth settings.
  - new FK indexes are reported as unused until production traffic uses them; these indexes were added specifically to satisfy FK performance guidance.

Transparent logo pass on 2026-06-08:
- Created `public/brand/auctionsq-mark-transparent.png`.
- Created `public/brand/auctionsq-wordmark-transparent.png`.
- Updated `BrandLogo` to use the transparent PNG files and removed the blend-mode workaround.
- Verified both generated PNGs are `Format32bppArgb` and contain transparent sampled pixels.
- Re-ran `node --test tests\*.test.mjs` and `npm run lint`; both passed.

Mobile UX foundation pass on 2026-06-08:
- Public mobile header now uses the compact transparent mark and a shorter login label.
- Admin mobile header now uses compact branding and icon-first controls.
- Admin and dashboard navigation now render as visible mobile chip grids instead of clipped horizontal scrollers.
- Main admin/dashboard content containers now use `min-w-0`; page-level horizontal overflow is clipped while table/filter internals keep their own scroll behavior.
- Removed negative tracking utilities from key public/profile headings.
- Playwright screenshots captured under `output/playwright/mobile-ux/`.
- Rendered checks covered public pages plus authenticated dashboard/admin routes at 390px and 1440px.
- Re-ran `node --test tests\*.test.mjs`, `npm run lint`, and `npm run build`; all passed.

Production admin/lifecycle validation on 2026-06-08:
- Created temporary production admin/user accounts and clearly named `Codex QA` data.
- Browser-validated admin login, category creation, product creation with image upload, auction creation, bid cancellation with reason, user blocking, order status update, and audit-log visibility on `https://auctionsq.vercel.app`.
- Browser-validated two-user auction lifecycle:
  - incomplete profile was blocked from bidding.
  - profile completion unlocked bidding.
  - user one placed a bid.
  - user two outbid user one.
  - blocked user could not continue bidding.
  - expired auction finalization created a winner order for user two.
  - winner could view the order in the dashboard.
- Validated unsold-auction relist and exact relisted-auction cancellation through the admin UI.
- Cleaned up temporary production data:
  - remaining `Codex QA Product%` products: 0.
  - remaining `codex-qa-%` categories: 0.
  - temporary auth users deleted.

Performance/logical review on 2026-06-08:
- Confirmed no remaining negative tracking utilities in `src`.
- Confirmed no stale `NjeKlik`/`NjëKlik` app references remain in `src`, `public`, or `supabase`.
- Tightened Supabase list projections for products, auctions, bids, orders, profiles, and audit logs so admin/dashboard screens fetch only fields they render.
- Kept existing row limits on broad list reads; no unbounded admin table fetches were found in the reviewed launch paths.
- Restarted the local dev server and smoke-tested public `/auctions` plus authenticated dashboard/admin routes at 390px and 1440px.
- Cleaned up the temporary auth users created for the local smoke pass.
- Re-ran `node --test tests\*.test.mjs`, `npm run lint`, and `npm run build`; all passed.

Final review/deployment validation on 2026-06-08:
- Final local gates passed:
  - `node --test tests\*.test.mjs`: 18/18 tests.
  - `npm run lint`: no warnings or errors.
  - `npm run build`: passed.
  - `npm audit --json`: 0 vulnerabilities.
  - `git diff --check`: no whitespace errors; only Windows LF-to-CRLF warnings.
- Deployed production with `npx vercel deploy --prod --yes`.
- Vercel production alias updated to `https://auctionsq.vercel.app`.
- Production E2E after deployment passed:
  - admin login, category/product/image/auction creation.
  - incomplete profile bid block, profile completion, two-user outbid.
  - suspicious bid cancellation with reason.
  - blocked user bid prevention.
  - auction finalization, winner order creation, winner order view, admin order status update, audit-log visibility.
  - unsold-auction relist and relisted-auction cancellation.
- Cleaned up final production QA data:
  - temporary orders, bids, auctions, products, categories, storage object, and auth users removed.
  - remaining `Codex QA Product%` products: 0.
  - remaining `codex-qa-%` categories: 0.
- Production public mobile/desktop checks passed after cleanup for home, auctions, categories, how-it-works, FAQ, and contact.
- Final static review found no stale NjeKlik references, no remaining negative tracking utilities, and no unexpected direct bid/order/profile writes in reviewed app paths.

## Completed Work

- Rebranded the app metadata, public header/footer, dashboard header, auth pages, FAQ, how-it-works, and contact details from the copied Oferto direction to NjeKlik.
- Added missing user dashboard routes:
  - `/dashboard/profile`
  - `/dashboard/addresses`
  - `/dashboard/change-password`
- Added guided profile completion, delivery details, and password update forms connected to existing Supabase/server actions.
- Added admin dashboard structure and pages:
  - `/admin`
  - `/admin/products`
  - `/admin/auctions`
  - `/admin/bids`
  - `/admin/orders`
  - `/admin/users`
  - `/admin/categories`
  - `/admin/audit-logs`
  - `/admin/settings`
- Connected admin pages to product creation/upload, product update, auction creation, auction cancellation/relisting, bid cancellation, order status updates, user blocking, category creation, audit review, and expired-auction closing.
- Added admin authorization before the manual expired-auction closer can run.
- Updated the main category structure to:
  - Teknologji
  - Gaming
  - Pajisje Shtepiake
  - Vegla Pune
  - Kuzhine
  - Kozmetike & Pajisje
  - Sport & Fitness
  - Per Femije
  - Te Ndryshme
- Expanded Supabase seed data and applied it to the live Supabase project:
  - 9 categories
  - 9 products
  - 9 auctions
- Applied and verified live hardening migrations:
  - `update_own_profile` exists and is executable by authenticated users.
  - `confirm_order_delivery` exists and is executable by authenticated users.
  - direct authenticated bid insert policy is removed.
  - `auctions_one_live_per_product_idx` exists.
  - duplicate active/scheduled auction groups: 0.
- Ran real authenticated Supabase attack-path tests with temporary users/data:
  - direct profile role update blocked.
  - profile RPC allowed.
  - direct bid insert blocked.
  - `place_bid` RPC allowed.
  - direct order update blocked.
  - order delivery RPC allowed.
  - duplicate live auction blocked.
- Deployed the verified app to Vercel production at `https://auctionsq.vercel.app`.
- Browser-validated production with Playwright:
  - public home and auction detail return 200.
  - no visible/accessibility `NjeKlik` branding remains.
  - logged-out bidding requires login.
  - normal user login redirects to `/dashboard`.
  - normal user is blocked from `/admin`.
  - normal user can place a bid.
  - admin login redirects to `/admin`.
  - no browser console errors in the validated flow.
- Fixed production hydration warnings found during browser validation:
  - countdowns calculate after client mount.
  - home auction grid is server-rendered.
  - polling refresh no longer runs during initial hydration.
  - bid form renders a stable placeholder before client mount.

## Remaining Release Work

- Enable Supabase Auth leaked-password protection in the dashboard.
- Keep using the client-provided NjeKlik logo artwork; current transparent PNGs are generated from the uploaded/source NjeKlik files.
- Configure Vercel WAF/rate limits for edge-level protection before requests reach Server Actions.
- Configure production SMTP/from-name branding in Supabase Auth once the final domain/email is ready.

## Important Decisions

- NjeKlik remains an admin-controlled auction platform, not a marketplace.
- Only admins can create products and auctions.
- Users must be logged in and have complete delivery details before bidding.
- Supabase is the source of truth for live data; local memory data remains a fallback for sandbox/dev behavior.
- Temporary production data is approved for release validation and must be clearly named and cleaned up where safe.
- Uploaded logo files are the source for transparent NjeKlik logo assets.

Active task on 2026-06-22:
- Fixed VPS auth 502 root cause outside the repo by increasing nginx proxy buffers for Supabase/Next auth response headers.
- Updated app metadata to use the production punycode domain and the existing NjeKlik transparent mark as the browser/apple icon.
- Replaced the actual app `favicon.ico` with a NjeKlik mark icon because browsers prefer `/favicon.ico` over metadata icons.
- Latest checks for this metadata change: npm run lint passed; npm run build passed.
